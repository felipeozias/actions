on: push
name: 🔰 Build on push
# run-name: 🔰🔰 ${{ github.actor }} is learning GitHub Actions

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: Building project
      - run: echo Felipe Ozias
