name: 🔰 Build on push
run-name: 🔰🔰 ${{ github.actor }} is learning GitHub Actions - ${{ secrets.TEST }} 💥

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código-fonte
        uses: actions/checkout@v2

      - name: Configurar Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "14" # ou a versão desejada

      - name: Instalar dependências
        run: yarn

      - name: Build da  aplicação
        run: yarn build

      - name: Fazer upload da pasta dist para o repositório
        uses: actions/upload-artifact@v2
        with:
          name: client
          path: dist

      - name: Enviar arquivo para o servidor Ubuntu
        run: scp -i ${{ secrets.PRIVATE_KEY }} dist/* ubuntu@ec2-54-232-47-224.sa-east-1.compute.amazonaws.com:/server/oziascip
